module.exports = addition;

// let man = false;

/**
 * Add two numbers
 * - This function accepts two integer numbers and return the sum of the given numbers.
 * @param {number} num1 - The first number is an integer
 * @param {number} num2 - The second number is an integer
 * @returns {number} - Sum of num1 and num2
 */
function addition(num1, num2){
    return num1 + num2;
}

function addTemp(num1, num2){
    return num1 + num2;
}
