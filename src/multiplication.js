module.exports = multiplication;


/**
 * Multiply two numbers
 * - This function accepts two integer nmbers and return the produt of the given numbers.
 * @param {number} num1 - The first number is an integer
 * @param {number} num2 - The second number is an integer
 * @returns {number} - Product of num1 and num2
 */
function multiplication(num1, num2){
    return num1 * num2;
}